<?xml version="1.0" encoding="utf-8"?>
<!-- Тёмная шапка с логотипом и «чернильными» подтёками из макета (экраны 02 и 03).
     Размеры как в макете: тёмная часть 86 dp под статус-баром, волна 70 dp на ширину 390 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimaryDark"
        android:gravity="center_vertical"
        android:minHeight="86dp"
        android:orientation="horizontal"
        android:paddingStart="20dp"
        android:paddingEnd="20dp">

        <ImageButton
            android:id="@+id/buttonBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/bg_circle_outline_light"
            android:contentDescription="@string/back"
            android:src="@drawable/ic_arrow_back"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/imageLogo"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="10dp"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_paw" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_title"
            android:textAppearance="@style/TextAppearance.Lapka.Wordmark" />
    </LinearLayout>

    <!-- Волна растягивается по ширине экрана, высота считается по пропорции 390:70 -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:importantForAccessibility="no"
        android:scaleType="fitXY"
        android:src="@drawable/ink_drips" />
</LinearLayout>
